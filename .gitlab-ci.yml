build:
  image: openjdk:8-jdk-alpine
  stage: build
  script:
    - ./gradlew spotlessCheck build #integrationTest
  artifacts:
    reports:
      junit:
        - build/test-results/test/TEST-*.xml
        - build/test-results/integrationTest/TEST-*.xml

pages:
  image: python:alpine
  before_script:
    - pip install mkdocs
    - pip install mkdocs-material
  stage: test
  script:
    - mkdocs build
    - mv site public
  artifacts:
    paths:
      - public